/**
 * K' Content Package — Command / Input Routing (兼容入口)
 *
 * 此文件已迁移到 commands/kdashCommands.ts，这里保留 re-export 兼容层。
 * 新代码应直接从 commands/kdashCommands.js 导入。
 */
export {
  KDASH_MOVE_LIST,
  KDASH_WIN_QUOTES,
  KDASH_AVAILABLE_ACTIONS,
  type KdashMoveEntry,
} from './commands/kdashCommands.js';
