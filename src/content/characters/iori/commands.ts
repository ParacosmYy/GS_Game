/**
 * Iori Content Package — Command / Input Routing (兼容入口)
 *
 * 此文件已迁移到 commands/ioriCommands.ts，这里保留 re-export 兼容层。
 * 新代码应直接从 commands/ioriCommands.js 导入。
 */
export {
  IORI_MOVE_LIST,
  IORI_WIN_QUOTES,
  IORI_AVAILABLE_ACTIONS,
  type IoriMoveEntry,
} from './commands/ioriCommands.js';
