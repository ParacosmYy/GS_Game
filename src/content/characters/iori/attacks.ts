/**
 * Iori Content Package — Attack Definitions (兼容入口)
 *
 * 此文件已迁移到 attacks/ioriAttacks.ts，这里保留 re-export 兼容层。
 * 新代码应直接从 attacks/ioriAttacks.js 导入。
 */
export {
  IORI_ATTACK_KEYS,
  getIoriFrameData,
  getIoriAttackFrameData,
} from './attacks/ioriAttacks.js';
