/**
 * Andy Content Package — Attack Definitions (兼容入口)
 *
 * 此文件已迁移到 attacks/andyAttacks.ts，这里保留 re-export 兼容层。
 * 新代码应直接从 attacks/andyAttacks.js 导入。
 */
export {
  ANDY_ATTACK_KEYS,
  getAndyFrameData,
  getAndyAttackFrameData,
} from './attacks/andyAttacks.js';
