/**
 * Terry Content Package — Hitbox / Hurtbox Data (兼容入口)
 *
 * 此文件已迁移到 hitboxes/terryHitboxes.ts，这里保留 re-export 兼容层。
 * 新代码应直接从 hitboxes/terryHitboxes.js 导入。
 */
export {
  TERRY_HITBOX_KEYS,
  getTerryHitboxOffsets,
  TERRY_ATTACK_FRAME_KEYS,
  getTerryAttackFrames,
} from './hitboxes/terryHitboxes.js';
