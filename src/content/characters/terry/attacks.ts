/**
 * Terry Content Package — Attack Definitions (兼容入口)
 *
 * 此文件已迁移到 attacks/terryAttacks.ts，这里保留 re-export 兼容层。
 * 新代码应直接从 attacks/terryAttacks.js 导入。
 */
export {
  TERRY_ATTACK_KEYS,
  getTerryFrameData,
  getTerryAttackFrameData,
} from './attacks/terryAttacks.js';
