/**
 * Ryo Content Package — Attack Definitions (兼容入口)
 *
 * 此文件已迁移到 attacks/ryoAttacks.ts，这里保留 re-export 兼容层。
 * 新代码应直接从 attacks/ryoAttacks.js 导入。
 */
export {
  RYO_ATTACK_KEYS,
  getRyoFrameData,
  getRyoAttackFrameData,
} from './attacks/ryoAttacks.js';
