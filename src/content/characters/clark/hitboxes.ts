/**
 * Clark Content Package — Hitbox / Hurtbox Data (兼容入口)
 *
 * 此文件已迁移到 hitboxes/clarkHitboxes.ts，这里保留 re-export 兼容层。
 * 新代码应直接从 hitboxes/clarkHitboxes.js 导入。
 */
export {
  CLARK_HITBOX_KEYS,
  getClarkHitboxOffsets,
  CLARK_ATTACK_FRAME_KEYS,
  getClarkAttackFrames,
} from './hitboxes/clarkHitboxes.js';
