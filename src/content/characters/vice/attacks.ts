/**
 * Vice Content Package — Attack Definitions (兼容入口)
 *
 * 此文件已迁移到 attacks/viceAttacks.ts，这里保留 re-export 兼容层。
 * 新代码应直接从 attacks/viceAttacks.js 导入。
 */
export {
  VICE_ATTACK_KEYS,
  getViceFrameData,
  getViceAttackFrameData,
} from './attacks/viceAttacks.js';
