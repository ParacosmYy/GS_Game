# CLAUDE.md — KOF2002 长期迭代执行手册

本文件是给长期协作 AI 使用的详细操作手册。`AGENTS.md` 是最高约束和入口，本文件负责把“每一轮到底怎么想、怎么拆、怎么验收、怎么提交”写清楚。

如果本文件和 `AGENTS.md` 冲突，以 `AGENTS.md` 为准。如果本文件和用户当前明确指令冲突，以用户当前明确指令为准。

## 1. 总目标

把当前项目从“可运行的 KOF 风格样机”推进到“尽可能接近 KOF2002 / KOF2002UM / QF 街机体验的原创 2D 格斗游戏工程”。

这里的“接近正版”不是复制商业素材或直接搬运受版权保护代码，而是对齐这些可工程化的体验指标：

- 输入响应：出招窗口、方向缓冲、按钮边沿、取消时机要稳定、清晰、可复现。
- 帧节奏：移动、攻击、受击、倒地、起身、转场要有接近街机格斗的 timing。
- 打击反馈：hitstop、shake、spark、SFX、受击位移、暂停层级要共同服务“拳头打中”的感受。
- 角色气质：头像、站姿、攻击动作、招式节奏、胜利表现要统一，不像临时拼图。
- 系统深度：防御、能量、MAX、取消、juggle、counter、投技、版边等机制要逐步数据化。
- 稳定性：构建、回放、状态重置、AI、输入、随机数都要能被测试和复盘。
- 可维护性：新增角色、招式、动画、资产不应该修改通用战斗系统。

## 2. 当前原则

当前技术栈是 TypeScript + Vite + Canvas 2D。默认先不换技术栈，除非触发技术栈决策门。

### 2.1 不能做的事

- 不直接复制商业游戏素材、音频、角色代码或可识别表达。
- 不把 MUGEN / IKEMEN / QF 的结构原封不动搬进项目。
- 不用一次“大重写”代替可验证的小步迁移。
- 不把角色专属逻辑写进 `combat/`、`entities/`、`input/` 的通用模块。
- 不为了加分堆功能，不做没有验收标准的改动。
- 不把失败的构建或测试包装成完成。

### 2.2 必须做的事

- 每轮先阅读 `AGENTS.md`、本文件和相关 `docs/`。
- 每轮先确认本轮目标属于产品、架构、稳定性、资产、手感还是流程。
- 每轮必须能回答：“这一轮让游戏更像正版在哪里？”
- 每轮必须能回答：“这一轮还没有解决哪些不像正版的问题？”
- 每轮必须运行质量门禁，或者明确说明为什么无法运行。
- 每轮必须用中文 Conventional Commit 记录原因、差异、验证、风险。

## 3. 文档地图

- `AGENTS.md`：最高约束，短而硬。
- `CLAUDE.md`：长期 AI 执行手册，细而可操作。
- `README.md`：人类入口和项目导航。
- `docs/architecture/current-architecture.md`：当前真实结构。
- `docs/architecture/module-boundaries.md`：模块边界和依赖方向。
- `docs/architecture/future-engine-architecture.md`：未来引擎和换栈决策。
- `docs/process/iteration-workflow.md`：每轮执行流程。
- `docs/process/role-prompts.md`：PM、玩家、研发、测试、架构师角色模板。
- `docs/process/quality-gates.md`：测试、构建、手测门禁。
- `docs/process/scoring.md`：千分制评分规则。
- `docs/product/authenticity-standard.md`：对标正版体验的验收标准。
- `docs/product/roadmap.md`：阶段路线图。
- `docs/reference/public-research-sources.md`：可参考资源与边界。
- `docs/archive/SCORE.history.md`：历史评分审计日志，只查证，不作为当前规则。

## 4. 每轮迭代总流程

每轮都按以下顺序执行。用户明确要求只做文档、只做分析或只做某个修复时，可以缩小范围，但不能跳过风险判断和验证说明。

### 4.1 启动检查

1. 查看 `git status --short`，确认是否有用户未提交改动。
2. 阅读本轮相关文档，不读取已经删除或归档的旧约束。
3. 明确本轮类型：
   - 产品体验迭代
   - 架构整理迭代
   - 稳定性修复迭代
   - 资产管线迭代
   - 文档治理迭代
   - 技术栈评估迭代
4. 写出本轮边界：改什么，不改什么。

### 4.2 研究

每轮开始前先查本地参考资料：

- `references/mugen/`：学习 MUGEN / IKEMEN 的数据组织、角色定义、状态机、动画帧、碰撞盒、工具链。
- 当前 `src/`：确认项目真实结构，不凭记忆修改。
- 当前 `docs/`：确认现行约束。

允许搜索公开资料，但只用于了解通用工程方法和公开项目实践。不要把搜索结果当作商业素材来源。

研究输出必须包含：

- 本轮可借鉴点。
- 本轮不能照搬的内容。
- 本轮落到当前项目后的模块归属。

### 4.3 多角色需求评审

每轮至少模拟或调用以下角色。若可以使用子 agent，则优先让不同子 agent 分别负责；若子 agent 不可用，则由主 agent 明确写出这些角色的结论。

- 产品经理：定义本轮价值、范围、验收标准、非目标。
- 玩家/文件用户：从实际体验角度指出最刺眼的问题和不可退化项。
- 研发：拆实现步骤、改动文件、测试策略。
- 测试/文档：补用例、补约束、确认旧规则是否冲突。
- 架构师：检查模块边界、长期维护成本、技术栈风险。

角色模板见 `docs/process/role-prompts.md`。

### 4.4 方案

动手前先写方案，方案至少包括：

- 本轮目标。
- 本轮不做什么。
- 涉及文件。
- 职责域归属。
- 验收标准。
- 回退方案。
- 对“接近正版”的贡献。
- 对稳定性的影响。

如果是代码迭代，方案必须确认是否需要测试先行。涉及核心逻辑时优先测试先行。

### 4.5 实施

实施时遵守这些限制：

- 小步修改，不混入无关重构。
- 通用系统只接收数据，不认识具体角色。
- 角色数据只能放在 `characters/` 或未来角色数据目录。
- 资产转换和分析工具放在工具层，浏览器运行时只消费转换结果。
- 调试显示和正式渲染分层，不把 debug box 当作真实美术。
- 所有新增状态说明归属、生命周期、reset、snapshot/replay 影响。

### 4.6 验证

提交前至少运行：

```bash
npx tsc --noEmit
npx vite build
```

如果改动涉及核心逻辑，还要运行相关 `vitest`。如果改动涉及体验但无法自动测试，要写清楚手测路径。

验证结果必须写入提交正文和最终汇报。

### 4.7 复盘

每轮结束必须复盘：

- 本轮目标是否完成。
- 本轮更像正版在哪里。
- 本轮仍不像正版在哪里。
- 是否引入新的架构债。
- 是否需要更新约束文档。
- 下一轮最该做的一件事。

## 5. 每轮回顾清单

每次迭代完成前，用这份清单自查。

### 5.1 产品回顾

- 本轮是否解决了一个真实玩家能感知的问题？
- 这个问题是打击感、肖像、输入、稳定性、流程还是架构？
- 验收标准是否能被复测？
- 本轮是否避免了“只是看起来多了点东西，但体验没变好”？

### 5.2 正版接近度回顾

- 输入是否更稳？
- 帧节奏是否更清楚？
- 打击反馈是否更有层次？
- 角色气质是否更统一？
- HUD、选人、转场、胜利流程是否更像街机格斗？
- 当前差距最大的前三项是什么？

### 5.3 工程回顾

- 是否保持依赖方向？
- 是否消除了硬编码？
- 是否新增了可测试点？
- 是否新增了不可解释的全局状态？
- 是否破坏了构建、类型检查或资源 fallback？
- 是否为未来迁移留下了清晰接口？

### 5.4 文档回顾

- 本轮是否让某条约束过期？
- 是否新增了必须记录的决策？
- 是否有重复、冲突、误导性的文档需要删除或归档？
- `AGENTS.md` 是否仍然短而权威？
- `CLAUDE.md` 是否仍然详细但不和 `AGENTS.md` 打架？

## 6. 产品经理工作法

任何体验迭代都先写 PM brief。

```text
【问题】
当前玩家看到/玩到的最大问题是什么？

【目标】
本轮完成后，玩家会明确感到哪里变好了？

【范围】
本轮改哪些模块和文件？

【非目标】
本轮明确不处理什么，避免范围膨胀？

【验收】
如何判断完成？包含自动验证和手测路径。

【正版对标】
本轮参考 KOF/QF 的哪类体验：输入、帧、打击、角色、流程、稳定性？

【风险】
可能破坏什么？如何回退？
```

好的 PM brief 应该很窄。比如“让 Ryo 近轻拳命中反馈更扎实”比“全面提升打击感”更适合一轮迭代。

## 7. 玩家/用户视角

玩家视角不关心代码漂亮不漂亮，只关心体验是否可信。

每轮都问：

- 第一眼是否像格斗游戏，还是像 debug demo？
- 人物站姿、头像、攻击动作是否统一？
- 命中时是否有重量，还是像贴图穿过贴图？
- 输入失败时玩家能否理解原因？
- 选人、开局、KO、胜利是否有仪式感？
- 帧率、构建、刷新、资源加载是否稳定？

如果玩家视角认为体验没有改善，本轮不能因为“代码变干净了”而给产品体验加分；只能记录为工程债降低。

## 8. 架构师视角

架构师每轮必须检查：

- 模块边界是否清晰。
- 数据和逻辑是否分离。
- 运行时和工具链是否分离。
- 当前 Canvas 2D 是否还能支撑目标。
- 是否出现换栈信号。
- 如果未来迁移到 PixiJS/WebGL/Godot/C++，本轮接口是否可保留。

### 8.1 技术栈决策门

只有满足以下任意条件，才考虑换栈：

- Canvas 2D 在目标分辨率、角色数量、特效层级下无法稳定 60 FPS。
- 现有渲染管线无法支持正式 sprite atlas、palette、blend、shader 或批量绘制。
- 输入、回放、战斗逻辑已经抽离到与渲染无关，迁移不会重写全部游戏。
- 资产管线已经成形，换栈能复用数据。
- 用户明确要求进入换栈评估。

换栈不是逃避当前混乱的办法。未完成数据化、测试、资产边界前，大概率换栈只会把旧问题搬到新框架里。

### 8.2 推荐迁移方向

- 短期：继续 TS + Vite + Canvas 2D，修稳定性和数据边界。
- 中期：渲染层可评估 PixiJS 或 WebGL2，但 combat/input/state 保持纯逻辑。
- 长期：如果要做接近商用街机的工具链，可评估 Godot 或自研 WebGL runtime。
- 不建议直接 C++ 重写，除非已有稳定数据格式、测试集和明确发行目标。

## 9. 研发执行规则

研发只对本轮需求负责，不顺手大改。

### 9.1 文件归属

- `core/`：纯类型、常量、纯函数。
- `state/`：阶段、回合、选人、演出状态。
- `combat/`：判定、伤害、能量、取消、命中回调的逻辑事件。
- `entities/`：实体数据、物理、生命周期。
- `characters/`：角色数据、招式、帧、指令。
- `input/`：输入采样、解析、缓冲、通用指令匹配。
- `rendering/`：只读数据绘制，不持有战斗真相。
- `audio/`：响应事件播放，不决定战斗结果。
- `tools/`：离线资产转换、统计、校验。
- `docs/`：约束、方案、流程、评分。

### 9.2 新功能必须回答

- 数据放哪里？
- 逻辑放哪里？
- 谁拥有状态？
- 谁负责 reset？
- 怎么测试？
- 怎么回退？
- 未来换渲染层时是否能保留？

## 10. 测试与稳定性

稳定性优先级高于新增内容。

优先补这些测试：

- 输入缓冲和指令识别。
- 回放 determinism。
- RNG determinism。
- Fighter reset。
- 回合切换。
- combat hit / guard / counter / damage。
- meter / MAX / cancel。
- 资产 manifest 校验。

每次遇到 bug，不要直接猜修。先写复现路径，再定位状态归属，再修复，再补防回归验证。

## 11. 资产与肖像方向

当前肖像和人物观感差，是产品层面的核心问题。但不能靠随手画几个 canvas 图形解决。

长期方向：

- 建立合法原创 sprite / portrait 资产规范。
- 建立 `portrait manifest`：角色、尺寸、锚点、palette、fallback。
- 建立 `animation manifest`：动作名、帧序列、帧时长、偏移、碰撞盒。
- 建立 atlas 生成工具：把单帧 PNG 转成 spritesheet 和 JSON。
- 运行时只读 manifest，不在渲染里硬编码角色图形。
- 每个角色先做 idle、walk、jump、light/medium/heavy、hurt、knockdown、win 的最小闭环。

如果没有正式美术资产，允许使用原创 placeholder，但必须明确标注为 placeholder，并保持可替换接口。

## 12. 打击感方向

打击感不是一个函数，而是一组同步系统：

- 攻击帧数据：startup、active、recovery、cancel window。
- 命中事件：hit level、hitstop、hitstun、pushback、knockback。
- 摄像机：shake、zoom、screen freeze。
- VFX：spark 类型、位置、层级、持续时间。
- SFX：轻/中/重/防御/Counter/KO 分层。
- 受击动画：受击帧、硬直、击退、倒地。
- HUD：combo、counter、damage、meter 反馈。

每轮只改一小段打击感，并用样板角色验证。不要同时改所有角色。

## 13. 评分规则

当前项目采用 1000 分目标制，每轮最多 +5 分。

加分必须满足：

- 有明确需求。
- 有代码或文档可审计差异。
- 有验证结果。
- 有“更接近正版”的解释。
- 有剩余差距说明。

不得加分的情况：

- 只改了格式但没有降低风险。
- 构建或测试失败。
- 没有验收标准。
- 做了无关功能堆叠。
- 复制了不该复制的素材或代码。

文档治理可以加分，但只能在它真正降低长期迭代风险时加分。

## 14. Commit 规范

Commit 标题使用：

```text
type(scope): 中文标题
```

正文使用：

```text
原因：
- ...

差异：
- ...

验证：
- ...

风险：
- ...
```

常用 type：

- `docs`
- `feat`
- `fix`
- `refactor`
- `test`
- `chore`

提交前必须先验证。不要先提交再验证。

## 15. 下一阶段优先路线

### 阶段 A：文档与稳定性

- 清理冲突文档。
- 固化 AGENTS/CLAUDE/process/product/architecture 文档层级。
- 补 replay/input/rng/reset 测试。
- 把评分、路线图、验收标准稳定下来。

### 阶段 B：资产管线

- 定义 sprite manifest。
- 定义 portrait manifest。
- 定义 animation frame data。
- 定义 hitbox/hurtbox 数据格式。
- 做一个离线 atlas 生成工具。

### 阶段 C：Ryo 样板

- 用 Ryo 跑通站姿、移动、攻击、受击、倒地、胜利。
- 用一套真实 frame data 模型驱动视觉和判定。
- 打通 hitstop、shake、spark、SFX、HUD 反馈。

### 阶段 D：Kyo / Iori 扩展

- 复制样板结构，而不是复制代码。
- 招式、指令、帧数据全部角色数据化。
- 通用系统不出现角色名判断。

### 阶段 E：深层系统

- MAX。
- Free Cancel。
- Guard Cancel。
- Juggle。
- Counter Wire。
- Training / Replay UI。

## 16. 停止条件与升级条件

持续迭代不是永远乱改。遇到以下情况必须暂停实施，先给用户决策：

- 需要更换技术栈。
- 需要删除大量代码或资产。
- 需要引入大型依赖。
- 需要改变产品目标。
- 质量门禁持续失败且原因不明。
- 发现参考资料存在授权风险。

遇到以下情况应优先继续实施：

- 目标小、边界清、验证明确。
- 能降低稳定性风险。
- 能消除角色硬编码。
- 能让资产从 hardcode 走向 manifest。
- 能让战斗逻辑更可复现。

## 17. 给下一位 AI 的一句话

不要被“1:1 复刻”四个字带偏。真正能靠近正版体验的路径，是把输入、帧、判定、反馈、资产、流程一点点数据化、可测化、可替换化。每一轮只前进一个小闭环，验证它，提交它，然后再继续。
